# Cloudflare Pages Configuration
# This file configures the static SPA deployment on Cloudflare Pages.
# The Worker edge middleware has its own config at worker/wrangler.toml.

pages_build_output_dir = "dist"
